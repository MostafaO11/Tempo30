# 🎯 متتبع الإنتاجية | Productivity Tracker

تطبيق ويب لتتبع الإنتاجية مبني باستخدام Streamlit و Supabase.

![Arabic](https://img.shields.io/badge/Language-Arabic-green) ![Streamlit](https://img.shields.io/badge/Framework-Streamlit-red) ![Supabase](https://img.shields.io/badge/Database-Supabase-blue)

## ✨ المميزات

- 📊 **تتبع الإنتاجية** كل 30 دقيقة (48 فترة يومياً)
- 🎯 **نظام التقييم** من 0 إلى 4 مع ألوان مميزة
- 👥 **دعم متعدد المستخدمين** مع مصادقة آمنة
- 📈 **خريطة حرارة** بأسلوب GitHub
- 🔥 **سلسلة الإنتاجية** (Streak) لتحفيز الاستمرارية
- 📁 **فئات مخصصة** (عمل، دراسة، صحة، إلخ)
- 🌙 **الوضع الداكن** بتصميم عصري
- 🔤 **واجهة عربية** كاملة مع RTL

## 🚀 التثبيت

### 1. إنشاء مشروع Supabase

1. اذهب إلى [supabase.com](https://supabase.com)
2. أنشئ حساباً جديداً أو سجل الدخول
3. أنشئ مشروع جديد (New Project)
4. انتظر حتى يتم إنشاء المشروع

### 2. إعداد قاعدة البيانات

1. في لوحة تحكم Supabase، اذهب إلى **SQL Editor**
2. انسخ محتوى ملف `supabase_schema.sql`
3. الصق في المحرر واضغط **Run**

### 3. الحصول على مفاتيح API

1. في Supabase، اذهب إلى **Settings** → **API**
2. انسخ:
   - `Project URL` → هذا هو `SUPABASE_URL`
   - `anon public` key → هذا هو `SUPABASE_KEY`

### 4. إعداد المشروع محلياً

```bash
# استنساخ المشروع
cd "d:\mostafa\data science\autometino\protucdivity tracker"

# إنشاء بيئة افتراضية
python -m venv venv

# تفعيل البيئة (Windows)
.\venv\Scripts\activate

# تثبيت المتطلبات
pip install -r requirements.txt
```

### 5. إعداد متغيرات البيئة

أنشئ ملف `.env` في المجلد الرئيسي:

```env
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-public-key
```

### 6. تشغيل التطبيق

```bash
streamlit run app.py
```

سيفتح التطبيق في المتصفح على `http://localhost:8501`

## 📖 دليل الاستخدام

### تسجيل الدخول

1. افتح التطبيق
2. أنشئ حساباً جديداً أو سجل الدخول
3. ستصلك رسالة تأكيد على البريد الإلكتروني (إذا تم تفعيل ذلك في Supabase)

### تسجيل الإنتاجية

**الطريقة السريعة (نقرة واحدة):**
1. من لوحة التحكم، اختر الفئة
2. اضغط على زر التقييم المناسب (0-4)

**التسجيل اليدوي:**
1. اذهب إلى "تسجيل النشاط"
2. اختر التاريخ والفترة الزمنية
3. اختر الفئة والتقييم

### مستويات التقييم

| المستوى | الوصف | اللون |
|---------|-------|-------|
| 0 | لا إنتاجية | رمادي |
| 1 | منخفضة | برتقالي |
| 2 | متوسطة | أصفر |
| 3 | عالية | أخضر فاتح |
| 4 | ذروة الأداء | أخضر غامق |

### التحليلات

- **خريطة الحرارة**: تعرض متوسط الإنتاجية لكل ساعة في كل يوم
- **الاتجاهات**: رسم بياني للنقاط اليومية
- **تحليل الفئات**: توزيع النقاط حسب الفئات

## 📁 هيكل المشروع

```
productivity-tracker/
├── .streamlit/
│   └── config.toml          # إعدادات Streamlit
├── components/
│   ├── __init__.py
│   ├── sidebar.py            # الشريط الجانبي
│   ├── dashboard.py          # لوحة التحكم
│   ├── log_activity.py       # تسجيل النشاط
│   ├── analytics_page.py     # التحليلات
│   └── settings.py           # الإعدادات
├── styles/
│   └── custom.css            # التنسيقات
├── app.py                    # التطبيق الرئيسي
├── config.py                 # الإعدادات والثوابت
├── auth.py                   # المصادقة
├── database.py               # عمليات قاعدة البيانات
├── analytics.py              # حسابات التحليلات
├── requirements.txt          # المتطلبات
├── supabase_schema.sql       # سكربت القاعدة
└── README.md                 # هذا الملف
```

## 🔧 استكشاف الأخطاء

### خطأ في الاتصال بـ Supabase
- تأكد من صحة `SUPABASE_URL` و `SUPABASE_KEY` في ملف `.env`
- تأكد من تشغيل سكربت `supabase_schema.sql` بنجاح

### لا تظهر الفئات
- تأكد من تشغيل INSERT في سكربت القاعدة لإضافة الفئات الافتراضية

### مشاكل في المصادقة
- في Supabase، اذهب إلى **Authentication** → **Settings**
- تأكد من تفعيل Email Auth
- يمكنك تعطيل "Confirm email" للتطوير المحلي

## 📄 الترخيص

هذا المشروع للاستخدام الشخصي والتعليمي.

---

صُنع Mostafa Osama باستخدام Streamlit و Supabase
